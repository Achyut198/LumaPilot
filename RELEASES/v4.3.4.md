# LumaPilot v4.3.4

Maintenance release to fix update delivery and versioning consistency.

## Fixes
- App menu **Check for updates…** now runs the command-line update flow.
- `CFBundleVersion` now uses `$(CURRENT_PROJECT_VERSION)` so new builds show a real build bump.
- Latest install artifacts refreshed:
  - `dist/LumaPilot-v4.3.4-macOS.dmg`
  - `dist/LumaPilot-v4.3.4-macOS.dmg.sha256`
  - `dist/LumaPilot-latest-macOS.dmg`
  - `dist/LumaPilot-latest-macOS.dmg.sha256`

